{
  "name": "real-villa",
  "version": "1.0.0",
  "description": "Sito ASD Real Villa con shop, PayPal Business e Stripe",
  "main": "server.js",
  "scripts": {
    "test": "node --check server.js && node --check payments.js && node --check public/app.js && node --check public/shop.js && node --check public/checkout.js && node --check public/conferma-pagamento.js && node scripts/smoke-test.js",
    "start": "node server.js",
    "setup:paypal": "node scripts/create-paypal-plan.js",
    "setup:stripe": "node scripts/create-stripe-price.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "dependencies": {
    "dotenv": "^17.4.2",
    "express": "^5.2.1",
    "stripe": "^22.4.0"
  },
  "engines": {
    "node": ">=22.5.0 <27"
  }
}
